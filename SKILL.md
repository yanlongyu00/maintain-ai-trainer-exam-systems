---
name: maintain-ai-trainer-exam-systems
description: 维护用户售卖的人工智能训练师三级备考网页系统，覆盖理论刷题、实操训练和电脑端考场还原。用于处理“顾客反馈、页面打不开、题目或判分有误、错题或进度异常、手机布局问题、Python 运行卡住、需要修改/审查/本机预览/预览链接/打包/发布/上线 Netlify 或妙搭、核验正式与备用链接”等请求。也用于生成顾客回复话术、预览链接和发布前回归报告。
---

# 备考系统维护与发布

把顾客反馈变成可追溯的诊断、最小修复、回归审查和双平台发布。默认保护已售卖产品、用户本地进度和线上可用性。

## 先加载正确资料

每次触发先读 [references/systems.md](references/systems.md)。

- 遇到故障、题目、判分、数据、布局或性能问题，再读 [references/diagnosis-and-tests.md](references/diagnosis-and-tests.md)。
- 用户要求打包、发布、上线或核验线上版本，再读 [references/release.md](references/release.md)。
- 需要参考历史处理方式时读 [references/cases.md](references/cases.md)。

不要一次加载所有参考文件。

## 不可违反的原则

1. 先确认系统、链接、源码基线和影响范围，再改代码。
2. 用户只说“检查、分析、先讨论”时只诊断，不修改、不发布。
3. 即使用户要求修复，也先给出根因、最小方案和风险；获得“确认修改”后再写入。
4. 修复和回归通过后，必须先提供可点击的本机预览链接；预览不等于发布。
5. 用户确认预览结果并明确回复“确认发布”后，才允许改线上状态。
6. 不用文件名、修改时间或“看起来更新”推断权威版本。多份 HTML 不一致时先比较哈希、项目说明、已验证包和线上页面。
7. 不直接编辑发布副本，除非它已被证明确实是唯一权威源码。优先修改权威源码，再确定性同步到发布目录。
8. 不清空或重置用户的 `localStorage` 数据来掩盖问题。需要 schema 变化时设计迁移；无法恢复的数据要明确说明。
9. 不改变已经确认的判分口径、题库答案、访问控制或套餐边界，除非用户明确授权。
10. 顾客截图、姓名、身份证号、访问码、令牌和后台凭据不得写入公开页面、日志或顾客话术。
11. 修复一个页面时保护同一部署包内的其他页面；发布后分别验证正式入口和备用入口。
12. 区分“右侧标准答案”“本次实际运行结果”“判分依据”：三者来源和用途不同，不得用临时运行值冒充标准答案，也不得让固定展示值干扰同环境真实判分。
13. 用户界面沿用顾客能理解的稳定名称；差异说明放在差异发生的位置，不把内部来源术语堆到标准答案区。
14. 发布前明确每个平台由谁操作。用户只负责 Netlify 拖拽时，仍要确认妙搭是由助手发布还是本轮不更新；两个目标都完成验收后才能宣称双平台发布完成。

## 工作流

### 1. 受理反馈

从用户消息、截图和链接中提取：

- 对应系统：理论、实操或考场还原；
- 入口：Netlify 正式站或妙搭备用站；
- 设备、浏览器、网络、操作顺序、期望与实际结果；
- 是否可复现、是否影响数据、是否阻断使用；
- 用户当前只要诊断，还是已要求修改或发布。
- 涉及答案或输出时，用户认可的标准来源是什么；涉及发布时，Netlify 与妙搭分别由谁操作。

信息不全但可安全推进时，先检查本地资料和线上只读状态。只有缺失信息会改变修复方向或发布目标时才提一个最关键的问题。

### 2. 建立版本基线

1. 读取目标目录及其上级适用的 `AGENTS.md`、`CLAUDE.md` 或其他项目说明。
2. 运行：

```bash
python3 scripts/preflight.py --system <theory|hands-on|venue|all>
```

3. 比较候选源码、发布副本和历史已验证包的哈希与关键标记。
4. 只读检查用户给出的线上链接；记录页面标题、关键功能标记、控制台错误和网络失败。
5. 若本地候选互不一致，停止写入并先确定权威基线。不要用旧副本覆盖线上较新版本。

这些 HTML 是大型单文件应用，部分还内嵌 base64 图片。先用 `rg` 定向查函数名、常量、存储 key 和版本标记，再读取紧邻行；不要整文件输出到上下文。

### 3. 诊断根因

先复现，再从第一性原理拆解“正确结果必须满足哪些条件”。按以下类别归因：

- 用户操作或文案歧义；
- 题库来源、答案证据或题号映射；
- 题目身份、状态机、持久化或旧数据迁移；
- HTML/CSS/移动端布局；
- JavaScript、依赖、运行时或 CDN；
- 网络、DNS、缓存或托管线路；
- 主站与备用站版本漂移；
- 访问范围或权限。
- 标准答案、运行数据与判分数据被混为一谈；
- 文案解释位置不对，导致正确机制仍被顾客理解为错误。

输出诊断时明确列出：证据、根因、影响用户、最小修改、兼容方案、风险和验证方法。不要把推测写成已确认事实。

### 4. 等待“确认修改”

除非用户在当前请求中已明确批准按所述方案修改，否则停在诊断和方案。生产故障很急也不跳过这道门。

### 5. 实施最小修复

1. 先保留目标文件的可恢复副本或确认已有版本化副本。
2. 只改权威源码；不顺手重构无关区域。
3. 保留既有题库、套餐、访问码、进度和判分契约。
4. 需要同步多个发布副本时使用可复核的复制或构建步骤，并在同步后重新比较哈希。
5. 不覆盖用户未提交或未说明的其他改动。
6. 标准输出数值只从用户认可的报告、结果文件或原始练习资料提取；按页面既定格式处理小数位，并保留来源证据。来源缺失时停止，不用网页临时生成值代替。
7. 需要同时展示固定标准值与当前运行值时，在数据结构和渲染逻辑上分离；差异说明紧跟成功运行结果，右侧仍保持统一“标准输出”命名。

### 6. 验证和审查

按 [references/diagnosis-and-tests.md](references/diagnosis-and-tests.md) 执行与风险成比例的测试，至少包括：

- 原反馈场景复现；
- 正确路径和故意错误路径；
- 刷新后的状态恢复；
- 相邻功能回归；
- 手机与电脑关键布局；
- 语法、控制台和网络错误；
- 数据迁移和旧用户兼容；
- 正式/备用产物一致性。

然后分三遍审查：

1. **第一性原理审查**：数据身份、状态边界、依赖契约和失败回退是否成立。
2. **对抗式审查**：主动构造重复题号、未作答、慢网、断网、缓存旧版、切题竞态、窄屏、旧数据等失败条件。
3. **发布审查**：目标应用、文件层级、体积、访问范围、回滚物和线上验收清单是否正确。

只有证据通过才能称为“已修复”。若无法执行某项测试，明确写为未验证。

### 7. 生成预览并发送链接

回归通过后、询问发布前，必须执行：

1. 从已测试的候选产物生成干净预览目录，不从未经核验的发布副本启动。
2. 在本机启动临时 HTTP 服务，默认绑定 `127.0.0.1`，并先验证返回 HTTP 200。
3. 向用户发送可点击的 `http://127.0.0.1:<port>/` 链接，明确“仅当前电脑可访问，尚未发布线上”。
4. 保持预览服务运行，直到用户完成查看或本轮维护结束。
5. 用户提出预览反馈时，先确认最终交互边界；若改变已批准范围，重新等待“确认修改”。修改后重跑相关测试，并优先更新同一个预览链接。

不要为了给预览链接而提前部署 Netlify 或妙搭。用户需要手机预览时，先说明 `127.0.0.1` 只在当前电脑可用，再决定安全的预览方式。

### 8. 等待“确认发布”

向用户展示：预览链接、修改摘要、测试结果、残余风险、将要更新的 Netlify 项目和妙搭 app_id。只有用户明确回复“确认发布”后才发布。

### 9. 发布并线上验收

用户确认后按 [references/release.md](references/release.md) 执行：

1. 生成干净发布产物；保持 ZIP 根层级正确。
2. 先复述发布责任矩阵：目标平台、目标项目/app_id、操作者、当前状态。预览确认、“可以”或“就这样”不等于确认发布。
3. 先确认 Netlify 由谁操作。用户有多个账号或明确选择手动拖拽时，只生成并核验 ZIP；等待用户回复“上传完成”后再验收，不代替用户登录其他账号。
4. 复用现有妙搭 app_id 发布静态 HTML；不要为更新创建新应用。用户要求助手发布妙搭时，不要因为 Netlify 由用户操作而漏掉妙搭。
5. 核对妙搭运行时可见范围和托管差异，避免发布成功但顾客无权打开或误带不兼容脚本。
6. 分别下载正式与备用线上产物，比较版本、业务标记和允许差异；再进行真实页面抽查和旧缓存检查。

发布失败时停止后续外发，保留上一个可用版本，并说明失败点与恢复办法。

### 10. 交付结果

最终回复必须包含：

- 根因和本次修改；
- 已执行的测试及结果；
- 正式与备用链接；
- 是否保留进度、题库、访问控制和其他页面；
- 已知边界或未验证项；
- 可直接复制给顾客的简短话术；
- 必要时的强制刷新方式。

若 Netlify 由用户手动上传，先交付可点击 ZIP 路径、准确项目名和根层级清单；用户上传后再出最终双平台报告。

不要只说“已更新”“没有问题”。

## 常见触发示例

- “顾客反馈错题本里不是自己的题，先检查原因。”
- “实操系统手机展开后有一大段空白，做个预览。”
- “代码题一直运行中，修好后开启第一性原理和对抗式审查。”
- “确认修改，没问题后再给我打包。”
- “确认发布到 Netlify 和妙搭，并检查两个链接。”
